---
"@dudousxd/nestjs-durable": minor
---

`topology: { role: 'control-plane' }` accepts an optional `tenant`, mapped onto `namespace` — one `tenant` vocabulary for both roles, resolved to the right axis (operator poll-scoping vs worker queue-routing). Undefined = global operator, unchanged.
