---
'@dudousxd/nestjs-durable-core': patch
---

Throwaway changeset, used to exercise the snapshot publish path while verifying OIDC trusted
publishing. Delete before merging: it exists so `changeset version --snapshot beta` has something to
stamp, and carries no code change.
